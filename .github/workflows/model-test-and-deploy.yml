name: model-test-and-deploy
on:
  push:
    branches:
      - feat-gh-actions

jobs:
  validate-and-build:
    uses: CBIIT/bento-workflows/.github/workflows/model-test-and-deploy.yml@c01a5278200507f218135349e4a1855f4f0cd77e
    with:
      model_files: |
        icdc-model.yml icdc-model-props.yml
      model_prefix: icdc-model
    secrets:
      docker_repo: ${{ secrets.REPO }}
      docker_svc: ${{ secrets.RSVC }}
      repo_user: ${{ secrets.REPO_USER }}
      repo_pw: ${{ secrets.REPO_PW }}
